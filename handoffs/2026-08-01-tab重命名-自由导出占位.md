# 2026-08-01 — Tab 重命名 + 自由导出占位

## 完成的工作

- [x] **Tab 重命名**：TabBar 三 tab 新名称
  - 「多图层导出」→「统一导出」
  - 「批量导出」→「文字导出」
  - 新增「自由导出」占位
- [x] **App.vue**：自由导出 tab 渲染占位提示文字（无功能逻辑）
- [x] **构建验证**：完整构建通过
- [x] **文档同步**：CLAUDE.md（Tab 布局 + 统一导出/自由导出功能说明）、AGENTS.md（函数表补充）、doc/使用文档.md（三 tab 列表）
- [x] 上一轮完成：导出裁切修复（normalize + top-left resize + saveAs），已提交 `996b1ec`

## 进行中的工作

- 无

## 下一步计划

1. **实现自由导出功能**：用户选中图层 → 保留图层原始尺寸 → 各自导出为 PNG/JPG
   - 核心逻辑：创建 per-layer workDoc（尺寸 = layer.bounds + padding）→ duplicate layer → 导出 → 关闭，无需统一画布和锚点对齐
   - 参考：`ps-layer-tool/src/jsx/modules/export.ts` 的 `exportSingleLayer`（trim 归一化方案）
   - 宿主函数：新增 `freeExport(configJson)` 注册到 `$.HostScript`
   - 面板组件：新建 `FreeExportTab.vue`
   - 配置：prefix + format + padding + outputDir（不需要 anchor + canvas size）
2. 提交未推送的 tab 重命名变更

## 关键决策

- **三 tab 命名体系**：按画布策略区分 —— 统一导出（固定尺寸）、文字导出（固定尺寸 + 预设）、自由导出（原始尺寸）
- **自由导出 tab 先占位不实现**：用户明确要求，后续 session 再开发

## 已知问题 / 注意事项

- 自由导出占位在 `App.vue:18-21`，`v-else-if="activeTab === 'free'"`
- Tab id 映射：`layers`=统一导出、`batch`=文字导出、`free`=自由导出
- 默认 tab 仍为 `batch`（`getSetting("activeTab", "batch")`）

## 建议技能

- [ ] `/git-push` — 提交 tab 重命名 + 占位变更
